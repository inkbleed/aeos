import basic
import aeos
import brett
import examples
import matrak_ops
import matrak_dataload
import bookkeeper

startup:
    set username = Cloud
    set browser = chrome
    set retina = false
    set localdrive = c
    set tmpdir = c:\temp
    set mydir = C:\aeos
    echo Hi $USERNAME, your preferred browser is set to $BROWSER.

